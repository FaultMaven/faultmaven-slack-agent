apiVersion: v1
kind: Service
metadata:
  name: faultmaven-slack-agent
  namespace: faultmaven
  labels:
    app: faultmaven-slack-agent
spec:
  selector:
    app: faultmaven-slack-agent
  ports:
    - name: http
      port: 80
      targetPort: http
